<Page
    x:Class="CameraCOT.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CameraCOT"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" >

        <!-- <Image Source="C:\temp\CameraPhoto.jpg" />  -->

        <!-- <canvas:CanvasControl x:Name="canvas" Margin="689,0,105,581" CreateResources="canvas_CreateResources"/>  -->

        <CaptureElement x:Name="PreviewControl" Stretch="Uniform"/>

        <Image x:Name="imageElement" Margin="18,24,104,18" />
        <Image x:Name="imageElementNote" Margin="671,189,147,27" />

        <!-- <canvas:CanvasControl Width="500" Height="300" x:Name="canv"
            Draw="CanvasControl_Draw" CreateResources="CanvasControl_CreateResources" ClearColor="LightGray"/>-->



        <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Margin" Value="10,40"/>
                    <Setter Property="MinWidth" Value="80"/>
                    <Setter Property="MinHeight" Value="80"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="BorderBrush" Value="White"/>
                    <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                </Style>
            </StackPanel.Resources>
            <Button x:Name="getScenarioSettings" Click="getScenarioSettings_Click">
                <Viewbox MaxHeight="40" MaxWidth="40">
                    <SymbolIcon Symbol="Add"/>
                </Viewbox>
            </Button>
            <Button x:Name="SwitchCam" Click="SwitchCam_Click">
                <Viewbox MaxHeight="40" MaxWidth="40">
                    <SymbolIcon Symbol="Switch" RenderTransformOrigin="1,1"/>
                </Viewbox>
            </Button>
            <Button x:Name="PhotoButton" Click="PhotoButton_Click">
                <Viewbox MaxHeight="40" MaxWidth="40">
                    <SymbolIcon Symbol="Camera"/>
                </Viewbox>
            </Button>
            <Button x:Name="VideoButton" Click="VideoButton_Click">
                <Grid>
                    <Ellipse x:Name="StartRecordingIcon" Fill="Red" Width="20" Height="20"/>
                    <Rectangle x:Name="StopRecordingIcon" Fill="White" Width="20" Height="20" Visibility="Collapsed"/>
                </Grid>
                <!--<Viewbox MaxHeight="40" MaxWidth="40">
                    <SymbolIcon Symbol="Video"/>
                </Viewbox> -->
            </Button>
            <!--<Button x:Name="StopRecord" Click="StopRecord_Click">
                <Viewbox MaxHeight="40" MaxWidth="40">
                    <SymbolIcon Symbol="Stop" RenderTransformOrigin="1,1"/>
                </Viewbox>
            </Button> -->
            

        </StackPanel>

        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Center">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Margin" Value="10,40"/>
                    <Setter Property="MinWidth" Value="80"/>
                    <Setter Property="MinHeight" Value="80"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="BorderBrush" Value="White"/>
                    <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                </Style>
            </StackPanel.Resources>
            <Button x:Name="inReportRead" Click="inReportRead_Click">
                <Viewbox MaxHeight="40" MaxWidth="40">
                    <SymbolIcon Symbol="Read"/>
                </Viewbox>
            </Button>
            <Button x:Name="runMeasure" Click="runMeasure_Click">
                <Viewbox MaxHeight="40" MaxWidth="40">
                    <SymbolIcon Symbol="Accept"/>
                </Viewbox>
            </Button>
            <Button x:Name="_stopMeasure" Click="stopMeasure_Click">
                <Viewbox MaxHeight="40" MaxWidth="40">
                    <SymbolIcon Symbol="Account"/>
                </Viewbox>
            </Button>
            <Button x:Name="_getDistanse" Click="getDistanse_Click">
                <Viewbox MaxHeight="40" MaxWidth="40">
                    <SymbolIcon Symbol="Go"/>
                </Viewbox>
            </Button>
        </StackPanel>

        <StackPanel Orientation="Horizontal"  VerticalAlignment="Top" HorizontalAlignment="Center">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Margin" Value="10,40"/>
                    <Setter Property="MinWidth" Value="80"/>
                    <Setter Property="MinHeight" Value="80"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="BorderBrush" Value="White"/>
                    <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                </Style>
            </StackPanel.Resources>

            <Button x:Name="mainCameraButton" Click="mainCameraButton_Click">
                <Viewbox MaxHeight="40" MaxWidth="40">
                    <SymbolIcon Symbol="Accept"/>
                </Viewbox>
            </Button>
            <Button x:Name="endoCameraButton" Click="endoCameraButton_Click">
                <Viewbox MaxHeight="40" MaxWidth="40">
                    <SymbolIcon Symbol="Accept"/>
                </Viewbox>
            </Button>
            <Button x:Name="termoCameraButton" Click="termoCameraButton_Click">
                <Viewbox MaxHeight="40" MaxWidth="40">
                    <SymbolIcon Symbol="Accept"/>
                </Viewbox>
            </Button>
            
        </StackPanel>

        <TextBox x:Name="textBoxInfo" HorizontalAlignment="Left" Margin="230,833,0,0" Text="TextBox" TextWrapping="Wrap" VerticalAlignment="Top" Height="67" Width="315"/>
        <Frame x:Name="ScenarioFrame" Margin="270,500,166,0"/>

    </Grid>
</Page>
